spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/demodb
    username: postgres
    password: rukundo
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

server:
  port: 8087
